# SEDCLIP dependencies (versions pinned to the tested CMLSED environment, Python 3.9).
#
# NOTE: install torch/torchvision separately with a CUDA build that matches your
# machine, BEFORE `pip install -r requirements.txt`. Tested build (CUDA 11.8):
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu118

numpy==1.26.4
scipy==1.12.0
einops==0.8.0
transformers==4.44.0
opencv-python==4.10.0.84
pillow==10.4.0
ftfy==6.2.3
regex==2024.7.24
tqdm==4.66.5
packaging
